server:
  http_listen_port: 3200
  grpc_listen_port: 4317

storage:
  trace:
    backend: local
    local:
      path: /tmp/tempo/traces

ingester:
  wal:
    enabled: true
    dir: /tmp/tempo/wal

compactor:
  compaction:
    enabled: true

metrics:
  address: 0.0.0.0:9095
